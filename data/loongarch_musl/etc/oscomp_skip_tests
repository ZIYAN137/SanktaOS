# One test group tag per line to skip in OSCOMP rcS.
# Examples:
#   iozone-glibc
#   iozone-musl
#
# Lines starting with '#' and blank lines are ignored.

# iozone triggers ext4_rs write-path bugs in some setups; skip for now.
iozone-glibc
iozone-musl

# libcbench may hang the kernel/userspace; skip temporarily.
libcbench-glibc
libcbench-musl

# Already verified locally; skip for now to run later groups first.
basic-glibc
busybox-glibc
cyclictest-glibc
iperf-glibc
